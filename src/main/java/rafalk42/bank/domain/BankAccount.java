package rafalk42.bank.domain;

/**
 * Represents a single, valid account (a handler) on which operations can be performed using the Bank interface.
 */
public abstract class BankAccount
{
	public abstract String getId();
}
